{
  "name": "mcp-ssh-server",
  "version": "1.0.0",
  "description": "MCP server for SSH operations - provides full SSH access to remote systems",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-ssh-server": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "ssh",
    "remote",
    "server"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "ssh2": "^1.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/ssh2": "^1.15.0",
    "typescript": "^5.3.0"
  }
}
